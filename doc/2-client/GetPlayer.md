# GetPlayer

GetPlayer(attribut)
GetPlayer({attribut})

## Description

Function to get a player's attribute.

## Exemples

```lua
GetPlayer() -- Get all
GetPlayer("steamId") -- Get specific attribut
GetPlayer({"steamId"}) -- Get specific attribut
```
